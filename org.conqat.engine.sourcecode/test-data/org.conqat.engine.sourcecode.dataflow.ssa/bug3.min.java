public class A {
	public void a(List<String> x) {
        while (something()) {
        }
      
        if (m.find()) {
        	String newQuery = JSTOR_URL + m.group(1);
        	return newQuery;
  		}
        // there used to be a reference to a non-existing variable here: newQuery_-3
	}
}