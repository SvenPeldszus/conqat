// tests that the "static" modifier is parsed correctly

class Foo
{
	// the tokens are on separate lines so we can clearly see, which tokens are included in the entity
	// and which aren't
	public
	static
	void A() {
	}

}

