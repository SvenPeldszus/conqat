#include <iostream>

using std::cout;

int main () {
	int digit = 4;
	if (digit > 3 || digit == 3) {
		if (digit < 5) {
			cout << digit;
		}
	} else if (digit < 2) {
		cout << digit;
	} else {
		cout << digit;
	}
}

